# THE FOLLOWING IS AUTOMATICALLY GENERATED BY ANSIBLE
# This file describes the network interfaces available on your system
# and how to activate them. For more information, see interfaces(5).

# The loopback network interface
auto lo
iface lo inet loopback

# The primary network interface
auto {{ desired_IFACE }}
iface {{ desired_IFACE }} inet static
    address {{ desired_IP }}
    netmask {{ desired_MASK }}
    network {{ desired_NETWORK }}
    broadcast {{ desired_BROADCAST }}
    gateway {{ desired_GATEWAY }}
    dns-nameserver {{ desired_DNS }}
